name: data
description: Commons Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  freezed_annotation:
  json_serializable:
  http:

dev_dependencies:
  build_runner:
  freezed:
