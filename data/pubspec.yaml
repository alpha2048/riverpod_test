name: data
description: data package.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  freezed_annotation:
  json_serializable:
  http:

dev_dependencies:
  build_runner:
  freezed:
  lints: ^1.0.0
  test: ^1.16.0
